{
  "name": "my-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rmdir /S /Q assets",
    "deploy": "npm run build && cd dist && rename index.dev.html index.html && xcopy . .. /E /Y /I"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "vite": "^7.1.3"
  }
}
