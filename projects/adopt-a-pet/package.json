{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rmdir /Q /S assets",
    "deploy": "npm run build && cd dist && rename index.dev.html index.html && xcopy . .. /E /Y /I"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "dependencies": {
    "msw": "^2.10.5",
    "react-router-dom": "^6.30.1"
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }
}